const fs = require("fs");
const path = require("path");

const pagesDir = path.join(__dirname, "src/app");

const escortPageTemplate = (locationName) => `"use client";

export default function Page() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-pink-50 via-white to-pink-50">
      {/* Navigation */}
      <nav className="sticky top-0 z-50 bg-white/80 backdrop-blur-md shadow-sm border-b border-pink-100">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center h-16">
            <div className="flex items-center gap-2">
              <div className="w-8 h-8 bg-gradient-to-br from-pink-500 to-pink-700 rounded-lg flex items-center justify-center text-white font-bold">S</div>
              <span className="text-xl font-bold text-pink-600">Saumya</span>
            </div>
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <secconst fs = require("fs");
const path = require("path");

const pagesD  const path = require("pamd:text-6xl font-bold text-gray-9
const escortPageTemplate = (locationName) => `"t-t
export default function Page() {
  return (
    <div claspin  return (
    <div className="ti    <e}
         {/* Navigation */}
      <nav className="sticky top-0 z-50 bg-white/80 backdrop>
      <nav className="sr         <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div claable 24/7.
        </p>          <div className="flex justify-between items-center h-1Ad            <div className="flex items-center gap-2">
           /p              <div className="w-8 h-8 bg-gradient-toer              <span className="text-xl font-bold text-pink-600">Saumya</span>
            </div>
          </div>
        </div>
      </nav>

      {/* H E            </div>
          </div>
        </div>
      </nav>

      {cation          </div>
Ai        </div>
sc      </nav>
},
      {/* "Am      h", slug: "escorts-amconst path = require("path"eri", slu
const pagesD  const path = namconst escortPageTemplate = (locationName) => `"t-t
export default funccoexport default function Page() {
  return (
    <scorts-churchgate" },
  { name: "Co    <divlug    <div c-colaba" },
  { n         {/* Navigation */}
-d      <nav className="sticpa      <n: "escorts-ghatkopar" },
  { name: "Goregaon", slug: "          <div claable 24/7.
        </p>          <div className="flex justify-between hu        </p>         u" },
             /p              <div className="w-8 h-8 bg-gradient-toer            orts-kurla" },
  { name: "Lokhandwala", slug: "escor            </div>
          </div>
        </div>
      </nav>

      {/* H E            </div>
          </div>
        </div>
      </nav>

 "          </div>

         </div>
e       </nav>
 "
      {/* ine          </div>
        </div,         </div>
ma      </nav>
 n
      {catd",Ai        </div>
sc      </n {sc      </nav>
Ce},
      {/*  " scconst pagesD  const path = namconst escortPageTemplate = (locationName) => " export default funccoexport default function Page() {
  return (
    <scorts-chs-  return (
    <scorts-churchgate" },
  { name: "Co bh    <scor
   { name: "Co    <divlug g:  { n         {/* Navigation */}
-d      <nalu-d      <nav className="sticpe: "  { name: "Goregaon", slug: "          <div claable 24/7.
   :         </p>          <div className="r (const location of             /p              <div className="w-8 h-8 bg-gradient-toer            orts-xi  { name: "Lokhandwala", slug: "escor            </div>
          </div>
        </div>
      st          </div>
        </div>
      </nav>

      {/wr        </div>leP      </ortPage
      {(locat          </div>
        </div??        </div>cat      </nav>
e.
 "          

         </div>
✅e       <ully cr "
      {/* io s.        pages!`);
} catch (errma      </nav>
 n
      {cat:" n
      {catro essc  it(1);
}
